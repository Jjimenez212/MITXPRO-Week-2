var ball = create()
size(ball, 300, 300)
color(ball, 255, 0, 0)
move(ball, -300, -300)
colorRandom(ball)

/* var counter=0;
while (counter<10) {
    create();
    counter + 1;
} */

 var list = []; 

list.push(create());
list.push(create());
list.push(create());
list.push(create());
